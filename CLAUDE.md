# 인코딩 주의사항 — 소스 수정 전 반드시 확인

이 저장소의 한글이 포함된 소스 파일들(`.cpp`, `.h`, `.rc`, `.odl`, 그리고 `map_src/**` 아래 확장자 없는 모든 맵 파일)은 **CP949**(Windows 한글 코드페이지)로 저장되어 있고 **UTF-8이 아니다**. Read/Edit/Write 도구는 UTF-8을 전제로 동작하기 때문에, 이런 파일을 직접 Edit/Write로 건드리면 내가 손댄 줄뿐 아니라 **파일 전체의 한글 바이트열이 깨진다.** 실제로 3줄만 고쳤는데 400줄 넘게 diff가 발생한 사고가 있었다. 이 문제가 반복적으로 발생했으므로 아래 절차를 예외 없이 따른다.

## 파일 수정 전 필수 절차

1. **인코딩부터 확인한다.** `file <path>` 실행 — "UTF-8"이라고 안 나오면 CP949로 간주한다(이 저장소에서 EUC-KR/Shift-JIS 등 다른 레거시 인코딩은 확인된 바 없음). 확신이 안 서면 직접 변환 테스트:
   `iconv -f CP949 -t UTF-8 <path> | head -c 2000` 으로 한글이 깨지지 않고 정상 출력되는지 확인한다.
2. **UTF-8이 아닌 파일은 절대로 Edit/Write로 직접 건드리지 않는다.** 대신:
   - `iconv -f CP949 -t UTF-8 "<path>" > "<path>.utf8.tmp"` 로 임시 UTF-8 사본을 만든다
   - 그 `.utf8.tmp` 파일을 Read/Edit로 수정한다 (이제 정상 UTF-8이므로 안전)
   - `iconv -f UTF-8 -t CP949 "<path>.utf8.tmp" > "<path>"` 로 원본 위치에 CP949로 되돌려 쓴다
   - `.utf8.tmp` 파일은 삭제한다
3. **쓰고 난 뒤 반드시 diff 크기를 검증한다.** `git diff --stat -- "<path>"` 로 변경된 줄 수가 의도한 수정 범위(대개 몇 줄)와 비슷한지 확인한다. 몇 줄 고치려던 건데 diff가 수백 줄로 나오면 인코딩이 깨진 것이니 **즉시 `git checkout -- "<path>"`로 원복**하고 원인을 다시 파악한 뒤 재시도한다.
4. 이 절차는 `src/**/*.cpp`, `src/**/*.h`, `src/**/*.rc`, `src/**/*.odl`, 그리고 `map_src/**` 아래 모든 맵 소스 파일에 적용된다.
